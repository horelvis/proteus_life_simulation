# Configuración para acceso en intranet
# Copia este archivo a .env y ajusta los valores según tu red

# IP del servidor donde se ejecuta Docker
# Puedes obtenerla con: hostname -I | awk '{print $1}' (Linux)
# o con: ipconfig getifaddr en0 (macOS)
SERVER_IP=192.168.1.100

# Puertos externos (pueden cambiarse si están ocupados)
BACKEND_PORT=8000
WEBSOCKET_PORT=8765
FRONTEND_PORT=3000

# URLs para el frontend (usa la IP del servidor)
REACT_APP_BACKEND_URL=http://${SERVER_IP}:${BACKEND_PORT}
REACT_APP_WS_URL=ws://${SERVER_IP}:${BACKEND_PORT}
REACT_APP_ARC_WS_URL=ws://${SERVER_IP}:${WEBSOCKET_PORT}

# Configuración de CORS (permite acceso desde cualquier origen en la red local)
CORS_ORIGINS=["http://localhost:3000","http://localhost:3001","http://${SERVER_IP}:3000"]